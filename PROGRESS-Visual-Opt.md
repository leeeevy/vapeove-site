# VAPEOVE 视觉一致性优化 — 进度报告

> 分支：`optimize/visual-refresh-20260823` · Commit：`110ab27` · 变更：347 文件
> 日期：2026-08-23 · 状态：✅ 完成（本地已验证，未部署线上）

---

## 一、7 项原始优化项进度

| # | 优化项 | 状态 | 说明 |
|---|--------|------|------|
| 1 | 全局字体统一 | ✅ | Orbitron/Rajdhani → **Sora(标题)+Inter(正文)**，347 文件全覆盖，0 残留旧字体 |
| 2 | 保持线上首屏+产品区布局 | ✅ | 结构未动，仅调样式。截图#1、#2 布局保持不变 |
| 3 | 语言选择器美化 | ✅ | 白色简陋下拉 → 深色霓虹胶囊 + 地球图标 + 青色文字 + 自定义箭头 |
| 4 | 白色带光看不清文字 | ✅ | hero 标题/霓虹文字：降光晕、强核心、加描边（截图#4） |
| 5 | 撑满屏的 promo 横幅 | ✅ | 全宽贴合 → 容器内圆角卡片（截图#5） |
| 6 | 浏览器语言自动检测 | ✅ | 仅英文根页首访、未手选时软重定向；**SEO 无风险**（详见下） |
| 7 | 导航 404 修复 | ✅ | 找出并修复多处源站遗留死链（详见下） |

---

## 二、逐项详解

### 1. 全局字体统一
- **原因**：整站 4 语言 + 不同页面混用 Orbitron/Rajdhani，风格不统一，且 Orbitron 辨识度过高、正文易读性差。
- **方案**：Sora（标题，几何科技感、比默认字体有辨识度但远优于 Orbitron 易读）+ Inter（正文，欧洲通用度极高）。
- **覆盖**：德语 ä/ß、波兰语 ł/ż/ę、西语 ñ/é 全部原生支持。
- **验证**：`Orbitron/Rajdhani` 全站 0 残留，347 文件使用 Sora/Inter。

### 2. 保持线上布局
- 首屏 hero + 下方 4 款优质产品介绍卡片的结构、顺序、网格完全保留，未做任何布局调整，只优化了视觉样式。

### 3. 语言选择器（截图#3）
- 原：浏览器默认白色 `<select>`，地球图标＋白底下拉，与深色导航格格不入。
- 新：深色半透明青边胶囊、青色高对比文字、自定义 SVG 下拉箭头、hover 霓虹光。移动端自动隐藏。

### 4. 白色带光看不清（截图#4）
- 原：hero 标题用「白色字 + 7 层青色光晕」，在深蓝背景上光晕过曝、字芯发糊。
- 新：新增「READABILITY OVERRIDE」块——强化白色核心字重、把 7 层光晕降到 3 层柔光、增加边缘描边。**字更清晰，赛博感保留**。

### 5. 撑满屏的 promo 横幅（截图#5）
- 原：50K PUFFS 白色横幅 `width:100%` 撑满整屏，与深蓝背景冲突。
- 新：改为**容器内居中、max-width:1400px、圆角 + 边框 + 投影**的产品横幅卡片，与深蓝融合，不再是突兀满屏。
- 决策：**保留**（有产品营销价值），改成卡片式，而不是删除。

### 6. 浏览器语言自动检测（截图#3 相关功能）
- **是否影响 SEO？结论：不会。**
  - 实现：仅在**英文根页**（`/`、`/index`）且用户**从未手选语言**时，用 JS `location.replace` 做一次**软重定向**到对应语言路径。
  - 为什么安全：① 每门语言页都有独立 `rel=canonical` + `hreflang`，爬虫看到的对应关系完整；② Googlebot 通常把 `navigator.language` 视为 `en-US` 或空，会停在英文默认；③ 不用 301/302 服务端重定向，不改变爬虫索引的 URL。
- 默认语言：**英语**。德语/波兰语/西语用户在首访时被引导到对应语言。

### 7. 导航 404 修复（截图#6）
通过脚本审计全站（页面级死链 + 资源引用缺失），找出并修复了**源站遗留**的死链（非部署问题）：
- **de/pl/es 三个首页**：资源路径错误（`images/` 应为 `../images/`、`js/` 应为 `../js/`）、导航链接错误（`products.html` 应为 `../products.html` 等）——共 30+ 处。
- **de/blog 5 篇深度 3 层文章**：`../../../css` 多了一级，应为 `../../css`（否则 css/js/favicon 全 404）。
- **blog/how-to-pay 及 de/pl/es 版**：`../spain-disposable-vape-wholesale` 死链 → 同级 `spain-disposable-vape-wholesale.html`。
- **product-70k 4 个产品页**：引用不存在的 `product-70k-cyber-scene.jpg` → 替换为真实存在的 `hero-70k.jpg`。

**审计结果**：页面级死链 `0`，资源引用缺失 `0`。

---

## 三、自主决策记录

| 决策点 | 我的选择 | 理由 |
|--------|----------|------|
| 字体选型 | Sora + Inter | 有辨识度、易读、欧洲字符全覆盖 |
| SEO 处理 | 软重定向 + 尊重手选 + 保留 hreflang | 不用服务端 301/302，不破坏 SEO |
| promo 横幅 | 改成卡片，不删除 | 保留产品营销价值 |
| 悬空产品图 | 用 hero-70k.jpg 替换 | 避免缩略图重复 |
| CSS 缓存 | 统一 bump `v=20260823b` | 强制浏览器加载新样式 |

---

## 四、完成进度

- ✅ 全部 7 项优化完成
- ✅ CSS 语法平衡校验通过（392/392 + 198/198）
- ✅ JS 语法校验通过（node --check）
- ✅ Chrome headless 渲染预览确认视觉效果达标
- ✅ Git 已提交（分支 `optimize/visual-refresh-20260823`，commit `110ab27`）

## 五、待办 / 风险

- [ ] **未 push 到 GitHub**（等用户确认后推送，可用 `GIT_SSH_COMMAND="ssh -i ~/.ssh/id_ed25519_vapeove"`）
- [ ] **未部署到 Netlify 线上**（仅本地工作区 + commit，未触发 deploy）
- 语言检测为 JS 前端实现，禁用 JS/极少数爬虫会停留在英文默认，无风险。
